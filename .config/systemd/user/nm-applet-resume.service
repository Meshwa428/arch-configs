# ~/.config/systemd/user/nm-applet-resume.service
[Unit]
Description=Restart nm-applet after suspend
After=suspend.target

[Service]
Type=oneshot
ExecStart=/usr/bin/systemctl --user restart nm-applet.service

[Install]
WantedBy=suspend.target
